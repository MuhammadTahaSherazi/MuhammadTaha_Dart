void main() {
  int lc_number = 12345; // Input number
  int sum = 0;
  double tempNumber = 12345.0;//number; // Copy of the input number
  print("A1 ${tempNumber}");
  while (tempNumber > 0) {

    print(tempNumber % 2);

   // int digit = tempNumber % 10; // Extract the last digit
   // sum += digit; // Add the last digit to the sum
   // tempNumber ~/= 10; // Remove the last digit
    
   // print(tempNumber ~/= 10);
   

  }

  // print("Input: $number");
  // print("Output: Sum of digits: $sum");
}
